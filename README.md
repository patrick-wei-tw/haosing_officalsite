# 昊星科技靜態官網

第一階段版本為純 HTML、CSS、JavaScript 靜態網站，可直接部署到 GitHub Pages。

## 頁面

- `index.html`
- `about.html`
- `services.html`
- `products.html`
- `solutions.html`
- `contact.html`

## 本機預覽

可直接用瀏覽器開啟 `index.html`。若要用本機伺服器預覽：

```bash
python3 -m http.server 8000
```

再開啟 `http://localhost:8000/`。
